# Changelog

## v2.8 (20800) - 2026-08-17

- 修复 Android 13 以下因液态玻璃组件（miuix-blur）导致的闪退：新增能力检测，Android 13+ 保持液态玻璃，Android 10–12 自动回退为普通组件。
- 重构自动备份权限逻辑：删除设置页的两条存储权限授权按钮，改为启动时检查「所有文件访问 + 通知」权限，非阻塞、拒绝也能进入应用；设置页新增使用说明与「未授权必要权限」提示。
- 新增内核压缩包回滚：操作记录新增「刷入内核压缩包」条目类型；开启自动备份后，刷入 AnyKernel3 前自动备份 boot 与 dtbo；操作记录与自动备份记录右下角均支持一键回滚。
- 日志展示统一为 `源 -> 目标` 格式；回滚按钮改为矩形。
- 移除旧功能：百宝箱内「自动禁用 AVB2.0」「隐藏 AVB 状态」开关、首次启动/版本更新向导、模块安装逻辑（后端 + 元模块）、设置中「重新启动向导」按钮，并删除模块压缩包资产（APK 体积约从 25.1 MB 降至 22.6 MB）。
- 百宝箱新增「开启无线调试端口」：端口校验、通知权限检查、常驻通知（含停止按钮）与 adbd 启停。
- 云 G(O)KI 仓库增强：包信息新增「管理器:版本号」红色高亮、最新版本五角星标记，并按各管理器最新版本置顶排序。
- 新增中英文使用文档。

详见：docs/releases/v2.8.md

## v2.7 (20700) - 2026-08-04

- 迁移 KernelSU 3.2.5 Manager 的 MIUIx 风格、浮动液态玻璃底栏和主页面 Pager 切换逻辑。
- 重做启动检测页，统一首次启动和后续启动流程，移除旧向导页面。
- 首次启动支持 RKF 后端模块安装，并提供 MagicMountRS 重定向方案和 Meta-OverlayFS 挂载方案选择。
- 优化启动检测项和后端工具准备，覆盖 Root、Root 管理器、模块包、工作目录、RootService、分区工具、刷写工具、BusyBox、脚本和解包工具。
- 修复 boot/init_boot/vendor_boot 检测 fallback，修正 magiskboot 工作目录依赖和 vendor_ramdisk.cpio 兼容。
- 更新内置 arm64 magiskboot。
- 整理百宝箱解包入口和解包记录页面。
- 清理旧向导和不再调用的启动代码，避免冗余源码和缓存占用。

详见：docs/releases/v2.7.md

